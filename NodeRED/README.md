# Node-RED Flow

This folder contains the Node-RED flow configuration (.json) used to visualize real-time parking data, monitor slot occupancy, and display environmental parameters through an interactive dashboard.
